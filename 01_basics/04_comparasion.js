console.log(null>=0)
//return true
//reason if == and comparator works differently 
//meaning when == sign is used it gives false where as when =>is used it gives true as it converts the null to the 0 value 
//undefined gives value false 

//strict check 
//===
//datatype check and check the value also 